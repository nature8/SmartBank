@{
    /* Bundled globally in _Layout.cshtml (jQuery + jQuery Validate + Unobtrusive Adapter).
       This partial exists so pages can @await Html.PartialAsync("_ValidationScriptsPartial")
       without caring where the scripts actually load, matching the standard scaffolding convention. */
}
